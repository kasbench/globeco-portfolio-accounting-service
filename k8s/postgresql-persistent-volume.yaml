apiVersion: v1
kind: PersistentVolume
metadata:
  name: portfolio-accounting-service-postgres-pv
  labels:
    app: portfolio-accounting-service-postgres
    type: local
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: local-storage
  local:
    path: /mnt/disk1/postgres-portfolio-accounting-service
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - node-2